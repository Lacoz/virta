{
  "name": "@virta/core",
  "version": "0.0.2",
  "description": "Virta core DAG pipeline engine (TransformationContext, buildLevels, runPipeline)",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com",
    "access": "restricted"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.4.0",
    "vitest": "^2.1.1"
  }
}
